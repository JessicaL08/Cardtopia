<div class="container">

<div class="show-album-name-album">
  <h1><%= @album.name %></h1>
    <%= link_to edit_album_path(@album) do %>
      <%= image_tag("icons-nav/pencil_icon_white1.svg", class: 'show-pokemon-edit-icon') %>
    <% end %>
  </div>

  <%= render 'album_pokemons/searchbar' %>
  <%= render 'album_pokemons/search-types' %>
  <%= link_to "Rénitialise recherche", request.params.except(:type, :season_id, :name, :extension_id), class: 'btn btn-primary' %>
  <hr>

  <%= link_to "Mes cartes", request.params.except(:type, :season_id, :name, :extension_id), class: 'btn', style: "color: #fbfcfa; background: #1e3d59; border-radius: 8px; border: 1px; border-style: solid; cursor: pointer; margin-bottom: 12px; margin-top: 3px" %>

<div class="w-100" data-controller="suppression" id="liste-elements" data-suppression-target="listeElements">
    <ul class="d-flex flex-wrap justify-content-center m-0 p-0 row p-0">
      <% @pokemons.each do |pokemon| %>
        <li class="col-lg-3 col-md-4 col-sm-6 col-6">
          <div class="card-container">
            <div class="img-size">
              <% if pokemon.image != "/high.webp" %>
                <%= link_to image_tag(pokemon.image, class: "pokemon-image", id: "nolink"), album_album_pokemon_pokemon_path(@album.id, pokemon.id) %>
              <% else %>
                <%= link_to image_tag("Pika-no-image", class: "pokemon-image no-image", id: "nolink"), album_album_pokemon_pokemon_path(@album.id, pokemon.id) %>
              <% end %>
            </div>
            <div class="text-center mt-2">
              <%= check_box_tag "selected_pokemons[]", pokemon.id, false, { disabled: true, data: { suppression_target: "checkbox" }, style: "display: none;" } %>
            </div>
          </div>
        </li>
      <% end %>
    </ul>

    <button id="deleteSelectedButton" data-action="suppression#supprimerSelection" style="display: none;">
    <i class="fas fa-check" style="color: green;"></i>
</button>
<button id="cancelSelectedButton" onclick="location.reload();" style="display: none;">
    <i class="fas fa-times" style="color: red;"></i>
</button>
  </div>
</div>
